# building docker image
docker build -t myhelloapp .

# looking for image
docker images

# running image
docker run -d -p 5000:8000 myhelloapp
#5000:8000 represents port mapping

#goto browser and type
localhost:8000

# get info 
docker ps

# to stop, get container-id from docker ps 
docker stop <container-id>